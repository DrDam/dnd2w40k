\usepackage{graphicx}
\usepackage{tikz}
\usepackage{eso-pic}
\usepackage[most]{tcolorbox}
\tcbuselibrary{breakable}

\usepackage{etoolbox}
\makeatletter
\patchcmd{\part}{\thispagestyle{plain}}{\thispagestyle{plain}\setcounter{chapter}{0}}{}{\errmessage{Patch de \string\part\space a échoué}}
\makeatother

\usepackage{wallpaper}
\usepackage{float}
\usepackage{booktabs}

\setlength{\columnsep}{1.5cm}

\usepackage{caption}

\usepackage{cuted}

\usepackage[table]{xcolor}

% Types de colonnes pour les tableaux : largeur fixe, centrage vertical,
% alignement horizontal sans justification forcée
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}m{#1}}
\newcolumntype{C}[1]{>{\centering\arraybackslash}m{#1}}
\newcolumntype{R}[1]{>{\raggedleft\arraybackslash}m{#1}}

% ##### en Markdown -> \subsubsection (avec --top-level-division=part :
% # part, ## chapter, ### section, #### subsection, ##### subsubsection).
% Style demandé : pas de numéro, texte souligné (au lieu du gras seul
% par défaut). secnumdepth global reste à 5 (réglé par --number-sections
% côté pandoc) pour ne pas affecter la numérotation des autres niveaux
% -- seul le format de \subsubsection est changé, via titlesec.
\usepackage{titlesec}
\titleformat{\subsubsection}
  {\normalfont\bfseries}
  {}
  {0pt}
  {\underline}

\setcounter{tocdepth}{2}
