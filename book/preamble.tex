\usepackage{graphicx}
\usepackage{tikz}
\usepackage{eso-pic}
\usepackage[most]{tcolorbox}
\tcbuselibrary{breakable}

\usepackage{etoolbox}
\makeatletter
% \@part (classe extbook, cf. metadata.yaml -> documentclass: extbook)
% imprime TOUJOURS, en plus de l'argument #2 qu'on lui donne, sa
% propre ligne "\huge\bfseries \partname~\thepart" (ex: "Partie I")
% -- indépendamment du contenu de #2. C'est ce résidu (visible en
% petit, en noir, au milieu de l'image de couverture) qu'on neutralise
% ici : #2 est désormais notre encart \PartCoverBanner (voir
% book/macro/part_cover.lua et la section "PAGE DE PARTIE" plus bas
% dans ce fichier), qui affiche déjà le numéro de partie lui-même --
% cette ligne native ferait doublon.
% On retire UNIQUEMENT cette ligne (patch ciblé sur son texte exact
% dans extsizes/extbook.cls) : la numérotation de la table des
% matières (\addcontentsline, plus haut dans \@part, hors de ce bloc)
% n'est pas concernée et reste intacte.
%
% NOTE -- numérotation des chapitres : \part ne remet PLUS le compteur
% de chapitre à zéro (un patch le faisait ici auparavant, retiré sur
% demande) -- la numérotation des chapitres reste donc CONTINUE d'une
% partie à l'autre (chapitre 1, 2, 3... peu importe combien de parties
% séparent les chapitres), plutôt que de repartir à 1 à chaque
% nouvelle partie.
\patchcmd{\@part}
  {\huge\bfseries \partname~\thepart
   \par
   \vskip 20\p@}
  {}
  {}{\errmessage{Patch de \string\@part\space a échoué (label "Partie N" non trouvé -- vérifier la version de la classe extbook)}}
\makeatother

\usepackage{wallpaper}
\usepackage{float}
\usepackage{booktabs}

\setlength{\columnsep}{1.5cm}

\usepackage{caption}

\usepackage{cuted}
\usepackage{needspace}

\usepackage[table]{xcolor}

% supertabular : permet à un tableau de continuer sur la page/colonne
% suivante en répétant automatiquement sa légende et son en-tête de
% colonnes ("titre collant"). Contrairement à longtable, fonctionne en
% mode twocolumn (longtable lève une erreur fatale dans ce mode -- voir
% book/macro/tables.lua pour le détail de cette contrainte).
\usepackage{supertabular}

% Types de colonnes pour les tableaux : largeur fixe, centrage vertical,
% alignement horizontal sans justification forcée
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}m{#1}}
\newcolumntype{C}[1]{>{\centering\arraybackslash}m{#1}}
\newcolumntype{R}[1]{>{\raggedleft\arraybackslash}m{#1}}

% ============================================================
% TAILLES DE POLICE -- CONFIGURATION CENTRALISÉE
% ============================================================
% Point d'entrée UNIQUE pour ajuster toutes les tailles de police du
% document. Chaque commande ci-dessous est ensuite référencée par son
% NOM (jamais recopiée en dur) partout ailleurs dans ce fichier et dans
% book/titlepage.tex -- modifier une valeur ici suffit à la répercuter
% partout où elle est utilisée.
%
% Valeurs LaTeX standard, du plus petit au plus grand :
%   \tiny \scriptsize \footnotesize \small \normalsize
%   \large \Large \LARGE \huge \Huge
% \normalsize correspond à la taille du corps de texte (10pt par défaut
% pour la classe "book" sans option de taille -- voir classoption dans
% book/metadata.yaml si la taille du corps de texte elle-même doit être
% changée ; c'est un réglage distinct de ceux-ci, fixé par la classe
% document et non par une commande \newcommand).
%
% Exception volontaire : book/macro/statblock.lua (via
% \newtcolorbox{monsterblock}, plus bas dans ce fichier) applique déjà
% \tablefontsize à TOUT le contenu du bloc monstre via fontupper --
% \monstername/\monstersection en héritent donc automatiquement, sans
% avoir besoin de leur propre commande de taille de police en plus de
% leur \Large/\large de mise en emphase (voir ces commandes plus bas).

% --- Tableaux ---
% Contenu (en-têtes de colonnes + cellules de corps) et légendes
% (*Titre*{.table-title}) des tableaux, dans tout le document.
\newcommand{\tablefontsize}{\small}
\newcommand{\tablecaptionfontsize}{\normalsize}

% Espacement vertical ajouté après CHAQUE ligne de tableau (en-tête et
% corps, dans tous les modes -- standard, wide, grid, statline).
% Introduit en remplacement des filets horizontaux (\toprule/\midrule/
% \bottomrule, désormais entièrement retirés des tableaux du document,
% voir book/macro/tables.lua et book/macro/statblock.lua) : sans aucun
% filet, les lignes se touchaient visuellement sans cet espacement --
% ce \tablerowsep sert de séparation visuelle légère à la place.
\newcommand{\tablerowsep}{1.5pt}

% \tabcolsep RÉDUIT localement pour les tableaux "standard" (sans
% .wide ni grid -- book/macro/tables.lua, table_to_supertabular_lines) :
% le \tabcolsep par défaut (6pt) ajoute 2x\tabcolsep à la largeur RÉELLE
% du tableau à CHAQUE jonction entre colonnes adjacentes, sans être
% budgété par la largeur visée -- négligeable pour 2 colonnes, mais
% s'accumule au point de déborder dans la gouttière entre colonnes de
% page pour un tableau à 3 colonnes ou plus (ex : "Point d'expérience /
% Niveau / Bonus de maîtrise"). Même technique que \tablegridcolsep
% ci-dessous et \monsterblock/\statline dans book/macro/statblock.lua.
\newcommand{\tablecolsep}{2pt}

% Séparation entre les deux tableaux d'une paire "grid" (deux tableaux
% côte à côte sous une légende commune, voir captioned_table_grid dans
% book/macro/tables.lua) : par défaut, seul l'espacement standard de
% \tabcolsep (très fin) sépare les deux -- ajout d'un espacement dédié
% et d'un filet vertical léger pour bien marquer visuellement la
% coupure entre les deux tableaux.
\newcommand{\tablegridsep}{1.2em}   % espace de part et d'autre du filet
\newcommand{\tablegridrulecolor}{gray!40} % couleur du filet (même famille que le gris des lignes alternées, \tablerowsep ci-dessus)
% \tabcolsep RÉDUIT localement pour les deux tabulars imbriqués d'une
% paire "grid" (book/macro/tables.lua, captioned_table_grid) : chacun
% n'a que 2 colonnes (Score/Coût ou Score/Modificateur), donc 1 seule
% jonction interne -- le \tabcolsep par défaut (6pt) y ajoutait 12pt de
% largeur RÉELLE non budgétée par rapport à la largeur de contenu
% visée, contribuant (avec d'autres marges non comptées, voir
% captioned_table_grid) à un débordement dans la gouttière entre
% colonnes de page. Même technique que \monsterblock/\statline dans
% book/macro/statblock.lua.
\newcommand{\tablegridcolsep}{2pt}

% --- Titres de section (Markdown) ---
% Correspondance Markdown -> LaTeX : voir plus bas dans ce fichier
% (\subsubsection pour #####, \chapter pour # via --top-level-division=part).
\newcommand{\chaptertitlefontsize}{\Huge}   % \chapter (titre de chapitre)

% --- Table des matières ---
\newcommand{\tocfontsize}{\Huge}            % titre "Table des matières"

% --- Page de partie (book/macro/part_cover.lua) ---
% Taille du "PARTIE X" et du titre affichés dans l'encart en haut de
% l'image de couverture -- voir la section \PartCoverBanner plus bas
% dans ce fichier pour le détail du mécanisme.
\newcommand{\partcoverlabelfontsize}{\Large}  % "PARTIE X"
\newcommand{\partcovertitlefontsize}{\Huge}   % titre de la partie

% --- Bloc monstre (statblock.lua) ---
% Ces deux commandes ne fixent PAS la taille du CONTENU du bloc (déjà
% couverte par \tablefontsize via fontupper, voir note ci-dessus) --
% seulement l'emphase du nom du monstre et de ses sous-titres internes
% (Actions, Réactions...) par rapport à ce contenu.
\newcommand{\monsternamefontsize}{\Large}   % nom du monstre
\newcommand{\monstersectionfontsize}{\large} % sous-titres (Actions, ...)

% --- Page de titre (book/titlepage.tex) ---
\newcommand{\titlepageTaglineSize}{\Large}    % accroche sous le visuel
\newcommand{\titlepageTitleSize}{\Huge}       % titre principal (\BookSubtitle)
\newcommand{\titlepageCreditSize}{\large}     % mentions légales / crédits
\newcommand{\titlepageAboutTitleSize}{\Huge}  % titre "A propos"
% ============================================================


% #### et ##### en Markdown -> \subsection et \subsubsection
% (avec --top-level-division=part : # part, ## chapter, ### section,
% #### subsection, ##### subsubsection).
% Style demandé : pas de numéro, texte souligné (au lieu du gras seul
% par défaut), #### avec un filet plus épais que ##### pour marquer la
% hiérarchie (voir \subsectionrulewidth/\subsubsectionrulewidth plus
% bas). secnumdepth global reste à 5 (réglé par --number-sections côté
% pandoc) pour ne pas affecter la numérotation des autres niveaux --
% seul le format de \subsection et \subsubsection est changé, via titlesec.
%
% ATTENTION -- piège \underline et titres longs en mode twocolumn :
% \underline{...} (natif LaTeX2e, hérité du mode math) compose son
% contenu dans une \hbox -- une boîte RIGIDE, sans aucun point de
% coupure de ligne autorisé à l'intérieur. Un \subsubsection assez
% court tient dans la largeur de colonne par hasard, mais dès qu'un
% titre dépasse cette largeur (ex: "Modification sur une fiche
% personnage standard de D&D 5e."), la boîte ne peut PAS se replier sur
% plusieurs lignes : elle déborde tel quel par-dessus la colonne
% voisine, sans avertissement ni erreur de compilation (reproduit et
% confirmé en isolation, hors de tout pipeline Pandoc/Lua -- comportement
% de base de \underline, pas un artefact des filtres de ce projet).
% \soulpkg{soul} (\ul{...}) résout ce point nativement mais ajoute une
% dépendance non utilisée par ailleurs dans ce document.
%
% Deuxième piège -- \titlerule fait TOUJOURS pleine largeur de colonne :
% un premier correctif (remplacer \underline par \titlerule en 5e
% argument optionnel de \titleformat, APRÈS le titre) règle bien le
% débordement, mais \titlerule trace son filet sur \linewidth dans tous
% les cas, QUEL QUE SOIT le texte du titre au-dessus -- "Magie" (5
% lettres) se retrouvait donc souligné sur toute la largeur de la
% colonne, comme un titre 5x plus long (constaté en test, visuellement
% incohérent avec un soulignement "normal").
%
% Solution retenue (toujours sans nouveau package) : \titlerulewrap
% mesure (\settowidth) la largeur RÉELLE du titre tel que composé sur
% une seule ligne, et trace une règle de cette largeur précise -- sauf
% si cette largeur dépasse \linewidth (titre long qui se repliera de
% toute façon sur plusieurs lignes), auquel cas on retombe sur la
% pleine largeur de colonne, seul choix cohérent dans ce cas (le titre
% occupant alors plusieurs lignes, caler le filet sur la largeur d'une
% ligne précise -- ex: la dernière -- demanderait de mesurer une mise
% en page déjà figée, non disponible simplement sans dépendance
% supplémentaire telle que soul/ulem).
\usepackage{titlesec}
\usepackage{calc}

% --- Épaisseur des filets de titre (configurable) ---
% Même logique que le bloc de tailles de police plus haut : ces deux
% commandes sont le SEUL endroit à toucher pour ajuster l'épaisseur des
% traits sous les titres #### et #####. \subsectionrulewidth (####) est
% volontairement plus épais que \subsubsectionrulewidth (#####), pour
% marquer visuellement que #### est un niveau de titre au-dessus dans
% la hiérarchie -- tout le reste de leur traitement (gras, pas de
% numéro, largeur du trait calée sur le texte) reste identique entre
% les deux niveaux, voir \titlerulecore ci-dessous.
\newcommand{\subsubsectionrulewidth}{0.5pt}
\newcommand{\subsectionrulewidth}{1.5pt}

\newlength{\titleulwidth}

% Noyau commun : #1 = épaisseur du filet (une des deux commandes
% ci-dessus), #2 = texte du titre, injecté automatiquement par titlesec
% dans le "before-code" de \titleformat (voir plus bas). Factorisé ici
% pour que #### et ##### partagent exactement la même mesure de largeur
% et le même espacement filet/texte, seule l'épaisseur variant.
\newcommand{\titlerulecore}[2]{%
  \settowidth{\titleulwidth}{#2}%
  \ifdim\titleulwidth>\linewidth
    \setlength{\titleulwidth}{\linewidth}%
  \fi
  % \vspace{-0.6\baselineskip} (et non un \vspace positif) : sans ce
  % correctif, l'espace naturel laissé par \par après le titre (interligne
  % + profondeur de descente des caractères) crée un écart visuellement
  % disproportionné entre le texte du titre et le filet -- alors que
  % l'intention de départ (cf. l'ancien \underline) était un trait
  % "collé" au texte, façon soulignement classique. Valeur négative
  % choisie empiriquement pour coller le filet juste sous la ligne de
  % base du titre, quelle que soit sa taille de police.
  #2\par\nobreak\vspace{-1.25\baselineskip}\noindent\rule{\titleulwidth}{#1}%
}

% Wrappers à UN seul argument -- \titleformat n'injecte le titre composé
% que dans une macro à 1 argument (voir la 5e valeur des deux
% \titleformat ci-dessous) ; on ne peut donc pas passer l'épaisseur en
% argument à cet endroit-là, d'où ces deux wrappers minces, un par
% niveau de titre, qui referment \titlerulecore avec la bonne épaisseur.
\newcommand{\titlerulewrap}[1]{\titlerulecore{\subsubsectionrulewidth}{#1}}
\newcommand{\titlerulewrapThick}[1]{\titlerulecore{\subsectionrulewidth}{#1}}

\titleformat{\subsection}
  {\normalfont\bfseries}
  {}
  {0pt}
  {\titlerulewrapThick}

\titleformat{\subsubsection}
  {\normalfont\bfseries}
  {}
  {0pt}
  {\titlerulewrap}

\setcounter{tocdepth}{1}
\setcounter{secnumdepth}{0}

% met sur la même ligne le "chapitre X" et le nom du chapitre
\titleformat{\chapter}[hang]
  {\normalfont\chaptertitlefontsize\bfseries}
  {\chaptertitlename\ \thechapter\ —\ }
  {0pt}
  {}

% Espacement autour du titre de chapitre (avant / après / "indent" du
% bloc titre lui-même, dernier paramètre laissé à 0pt = pas de retrait).
% Par défaut, titlesec retombe sur des valeurs proches de book.cls natif
% (\vspace*{50pt} avant, \vskip 40pt après -- voir \@makechapterhead
% dans book.cls) dès que \titleformat est redéfini sans \titlespacing
% associée. Ces valeurs sont pensées pour un chapitre qui commence
% directement par du texte ; ici, le chapitre est souvent suivi d'une
% image (.wide), et le grand espace après-titre par défaut se voit
% comme un vide disproportionné au-dessus de l'image. Réduit ici à des
% valeurs plus resserrées, sans pour autant coller le titre au bord de
% page (le "avant" reste non nul pour respecter un minimum d'air en
% haut de page).
\titlespacing{\chapter}{0pt}{10pt}{10pt}

% ============================================================
% PAGE DE PARTIE -- ENCART DE TITRE (book/macro/part_cover.lua)
% ============================================================
% Comportement d'origine : le titre de partie (\part, niveau 1 Markdown
% via --top-level-division=part) s'affichait tel quel, centré
% VERTICALEMENT au milieu de l'image de fond -- comportement standard
% de book.cls (\null\vfil ... \vfil de part et d'autre du titre dans
% \@part/\@endpart). Résultat jugé décevant à l'usage : on veut un
% encart de titre façon livre de règles (bandeau en haut de l'image),
% pas un titre "flottant" au milieu.
%
% DEUX TENTATIVES RATÉES avant la solution retenue, notées ici pour ne
% pas les reproduire :
%
%   - Vider \titleformat{\part} pour rendre le titre invisible : ÉCHEC
%     -- \titleformat ne fait que changer le FORMAT (police, centrage,
%     label) appliqué autour du texte du titre ; il ne supprime jamais
%     le texte lui-même. Résultat observé : un titre minuscule, non
%     centré, mais toujours bien présent (juste sans mise en forme).
%
%   - Insérer \PartCoverBanner comme bloc SÉPARÉ juste après le Header
%     (dans part_cover.lua) : ÉCHEC -- \part se termine toujours en
%     interne par \vfil\newpage (\@endpart de book.cls). Tout ce qui
%     est inséré APRÈS le Header dans l'arbre Pandoc se retrouve donc
%     mécaniquement composé sur la page SUIVANTE, décalé de l'image de
%     fond (observé : le bandeau atterrissait sur la 2e page du couple
%     image+titre au lieu de partager la même page que l'image).
%
% SOLUTION RETENUE : ne pas toucher à \part du tout (le patch etoolbox
% de remise à zéro du compteur de chapitre, plus haut dans ce fichier,
% reste donc pleinement valide -- \part n'est jamais redéfini). À la
% place, \PartCoverBanner devient littéralement l'ARGUMENT AFFICHÉ de
% \part, en utilisant la forme standard à deux arguments de book.cls :
%
%   \part[titre court, pour la TdM/les signets]{contenu réellement affiché}
%
% part_cover.lua compose donc directement \part[Titre]{\PartCoverBanner{...}}
% en RawBlock (au lieu de laisser Pandoc générer \part{...} depuis le
% Header). Deux bénéfices immédiats :
%   - \PartCoverBanner est composé exactement au même endroit/à la
%     même page que l'aurait été le titre par défaut (puisque c'en est
%     littéralement l'argument) -- plus de décalage de page possible.
%   - book.cls utilise l'argument optionnel [#1] (notre titre lisible)
%     pour \addcontentsline{toc}{part}{\thepart\hspace{1em}#1} : la
%     table des matières et les signets PDF affichent le VRAI titre en
%     texte normal, indépendamment de ce qui est réellement composé
%     sur la page (notre encart, qui lui contient du code tikz/raw
%     LaTeX illisible tel quel -- inadapté à un signet PDF).
%
% \PartCoverBanner dessine l'encart en position ABSOLUE sur la page via
% un tikzpicture "remember picture, overlay" ancré sur (current
% page.north) -- même technique déjà utilisée par book/titlepage.tex
% pour son propre fondu noir. Un overlay tikz est indépendant du flux
% normal du document (aucun espace vertical consommé), donc sans
% interaction avec les pièges \clearpage/cuted documentés dans
% book/macro/newpage.lua.
%
% \arabic{part} (utilisé par part_cover.lua pour composer "PARTIE X")
% est fiable ici car \PartCoverBanner s'exécute À L'INTÉRIEUR du corps
% de \part, donc APRÈS le \refstepcounter{part} interne à \@part.
\newcommand{\partcoverboxwidth}{0.78\paperwidth} % largeur de l'encart
\newcommand{\partcoverboxyshift}{-2cm}           % distance depuis le haut de PAGE (pas de la colonne : \paperwidth/current page, indépendant du twocolumn)
\newcommand{\partcoverboxcolor}{black}           % fond de l'encart (même couleur que le fondu de titlepage.tex)
\newcommand{\partcoverboxopacity}{0.55}          % 0 = transparent, 1 = opaque

% #1 = numéro de partie déjà composé en LaTeX (ex: "PARTIE 1"),
% #2 = titre de la partie (déjà passé par pandoc.write coté Lua, donc
% gras/italique éventuels du Markdown source sont préservés),
% #3 = couleur du texte (reprend l'attribut textcolor="..." déjà
% utilisé par part_cover.lua -- même valeur, un seul encart à
% maintenir cohérent avec le reste du bandeau).
\newcommand{\PartCoverBanner}[3]{%
  \begin{tikzpicture}[remember picture, overlay]
    \node[anchor=north, yshift=\partcoverboxyshift] at (current page.north) {%
      \begin{tcolorbox}[
        enhanced,
        colback=\partcoverboxcolor,
        opacityback=\partcoverboxopacity,
        colframe=#3,
        boxrule=1pt,
        arc=2pt,
        width=\partcoverboxwidth,
        halign=center,
        left=14pt, right=14pt, top=10pt, bottom=10pt,
      ]
        \color{#3}%
        {\partcoverlabelfontsize\scshape #1}\\[4pt]
        {\partcovertitlefontsize\bfseries #2}
      \end{tcolorbox}%
    };
  \end{tikzpicture}%
}
% ============================================================
% TABLE DES MATIÈRES EN DEUX COLONNES
% ============================================================
% Par défaut, la classe book bascule automatiquement \tableofcontents
% en une seule colonne pleine largeur (\onecolumn), même en mode
% twocolumn -- c'est un choix de design intégré à la classe standard,
% PAS un effet du document ou de pandoc. On redéfinit ici
% \tableofcontents pour retirer cette bascule et garder le mode deux
% colonnes du document.
%
% Point d'attention technique : il ne suffit pas de retirer le
% \onecolumn/\twocolumn de la définition d'origine -- le titre "Table
% des matières" lui-même utilise normalement \chapter*, qui pour tout
% titre de chapitre non numéroté en mode twocolumn déclenche un AUTRE
% mécanisme interne de la classe (\@topnewpage, via \@schapter) qui
% force le titre en pleine largeur de PAGE avant de basculer le
% contenu suivant en deux colonnes. Pour l'éviter complètement, le
% titre "Table des matières" est ici composé manuellement (mêmes
% styles visuels que \@makeschapterhead, mais sans passer par
% \chapter*/\@schapter), pour qu'il s'insère normalement dans le flux
% de la colonne courante comme n'importe quel autre titre.
\makeatletter
\renewcommand\tableofcontents{%
    \@mkboth{\MakeUppercase\contentsname}{\MakeUppercase\contentsname}%
    \@starttoc{toc}%
    }
\makeatother
% ============================================================

% statblock-preamble.tex
% À inclure via --include-in-header avec le template LaTeX par défaut de Pandoc.
% Définit un cadre sobre "bloc monstre" + une ligne de caractéristiques centrée.

\usepackage[most]{tcolorbox}
\usepackage{array}

% --- Titre du monstre (remplace le H1 interne, sans numérotation ni TdM) ---
\newcommand{\monstername}[1]{%
  \par\vspace{2pt}
  {\bfseries\monsternamefontsize #1}\par
  \vspace{2pt}
}

% --- Sous-titre interne (Actions, Réactions, Traits légendaires...) ---
% Une fine ligne horizontale juste au-dessus du titre permet de
% détacher visuellement chaque section (Traits / Actions / ...) du
% texte qui précède, façon livre de règles -- sans quoi un sous-titre
% en simple gras se confondait avec le corps de texte environnant et
% pouvait sembler "ne pas s'afficher" même quand il était bien rendu.
\newcommand{\monstersection}[1]{%
  \par\vspace{6pt}
  \noindent\rule{\linewidth}{0.5pt}\par\vspace{3pt}
  {\bfseries\monstersectionfontsize #1}\par
  \vspace{2pt}
}

% --- Cadre sobre, façon livre de règles, sans bandeau coloré ---
% \tablefontsize (définie dans book/preamble.tex, = \small par défaut) :
% applique la même taille de police que les tableaux du document à TOUT
% le contenu du bloc monstre -- se propage automatiquement à monstername/
% monstersection/statline, qui sont émis à l'intérieur de ce groupe.
\newtcolorbox{monsterblock}{
  enhanced,
  breakable,
  colback=yellow!20,
  opacityback=0.95,
  colframe=black,
  boxrule=0.6pt,
  arc=1pt,
  left=8pt, right=8pt, top=6pt, bottom=6pt,
  before skip=12pt, after skip=12pt,
  fontupper=\tablefontsize,
}

% --- Ligne FOR/DEX/CON/INT/SAG/CHA ---
% Le tableau Markdown source devient un tabular Pandoc standard ;
% on encadre juste avec un peu d'espacement et un filet haut/bas discret.
% Pas de commande de taille de police ici : déjà héritée de
% \tablefontsize via fontupper (voir \monsterblock ci-dessus) --
% mise en dur ici auparavant (\small), ce qui la désynchronisait de
% \tablefontsize en cas de changement de ce dernier dans le bloc de
% configuration centralisé en haut de ce fichier.
\newenvironment{statline}{%
  \par\vspace{4pt}
  \noindent\hrulefill\par\vspace{2pt}
  \centering
}{%
  \par\vspace{2pt}
  \noindent\hrulefill\par\vspace{4pt}
}
% ============================================================
